import { storybookFailResult } from "./action-result";

export function createCustomLocation() {
  return Promise.resolve(storybookFailResult());
}

export function deleteCustomLocation() {
  return Promise.resolve(storybookFailResult());
}

export function getLocations() {
  return Promise.resolve(storybookFailResult());
}

export function updateCustomLocation() {
  return Promise.resolve(storybookFailResult());
}

export function updateLocationSelections() {
  return Promise.resolve(storybookFailResult());
}
