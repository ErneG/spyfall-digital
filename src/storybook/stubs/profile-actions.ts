import { storybookFailResult } from "./action-result";

export function deleteNameFromHistory() {
  return Promise.resolve(storybookFailResult());
}

export function getNameHistory() {
  return Promise.resolve(storybookFailResult());
}

export function getNameSuggestions() {
  return Promise.resolve(storybookFailResult());
}

export function getProfile() {
  return Promise.resolve(storybookFailResult());
}

export function updateProfile() {
  return Promise.resolve(storybookFailResult());
}
