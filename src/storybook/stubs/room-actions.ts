import { storybookFailResult } from "./action-result";

export function applyRoomContentSource() {
  return Promise.resolve(storybookFailResult());
}

export function createPassAndPlayRoom() {
  return Promise.resolve(storybookFailResult());
}

export function createRoom() {
  return Promise.resolve(storybookFailResult());
}

export function getRoomState() {
  return Promise.resolve(storybookFailResult());
}

export function joinRoom() {
  return Promise.resolve(storybookFailResult());
}

export function updateRoomConfig() {
  return Promise.resolve(storybookFailResult());
}
